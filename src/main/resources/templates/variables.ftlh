<!-- Special Variable -->
FreeMarker Version: ${.version}
Locale: ${.locale}
Lang: ${.lang}
TimeZone: ${.time_zone}
Now.date: ${.now?date}
Now.time: ${.now?time}
Now: ${.now?datetime}
Now: ${.now?iso_local}